module.exports = {
  /*
  options: {
    removeComments: true,
    collapseWhitespace: true
  },

  hp: {
    files: [{
      expand: true,
      cwd: 'homepage/' + config.hp.name + '/includes/',
      src: '*.html',
      dest: 'homepage/' + config.hp.name + '/toolkit/'
    }]
  },
*/
};