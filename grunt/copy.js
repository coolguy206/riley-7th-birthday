module.exports = function(grunt) {
  /*
    return {
      hpCopy: {
        files: {
          expand: true,
          cwd: 'homepage/' + config.hp.name + '/css',
          src: ['*.css'],
          dest: 'homepage/' + config.hp.name + '/include/'
        }
      }
    }
  }
  */
};